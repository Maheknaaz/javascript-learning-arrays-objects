// Arrays of numbers
let numbers = [10, 20, 30, 40, 50];
console.log(numbers);

// Arrays of strings
let colors = ["Black", "Blue", "Pink", "Purple"];
console.log(colors);

// Arrays of Boolean
let bool = [true, false, true, false];
console.log(bool);

// Mixed data types
 let mixed = [10, "hello", true, 5.6];
 console.log(mixed);

 // Arrays of objects
 let students = [
    {name: "Mahek", age: 21},
    {name: "Naaz", age: 22}
 ];

console.log(students);

// Arrays of Arrays(nested array)
let matrix = [
    [1, 2],
    [3, 4]
];

console.log(matrix);

// Empty arrays
let empty = [];
console.log(empty);
// console.log(empty.push("Hey"));
// console.log(empty);

// Array of Mixed complex types
 let details = [
    "Mahek",
    21,
    {city: "Hubli"},
    ["HTML", "CSS", "JS"]
 ];

console.log(details);